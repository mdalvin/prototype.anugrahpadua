import { About, CompanyValues, ContactUs, Gallery, VnM } from "../components";

export default function ContactPage() {
  return (
    <main className="h-full w-full bg-white">
      <ContactUs />
    </main>
  );
}
